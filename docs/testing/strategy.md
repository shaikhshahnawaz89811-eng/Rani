# Testing Strategy

Each core contract receives unit tests first. Feature integration tests should be added before replacing demo adapters with real runtimes, Git backends or local model engines. Manual validation must cover window lifecycle, editor save, terminal commands, Git confirmation, AI chat and responsive desktop layout.
